# Copy to secret.yaml and fill in real values — secret.yaml is gitignored
apiVersion: v1
kind: Secret
metadata:
  name: unmuted-secret
  namespace: unmuted
type: Opaque
stringData:
  AUTH_TOKENS: "change-me,another-token"
